import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { Comments } from './Comments';

<Meta title="Components/Content/Comments" component={Comments} />

# Comments

Comments component.

<Props of={Comments} />

export const Template = (args) => (
  <Comments comments={args.comments} />
);

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      comments: [{user: 'User 1', comment: 'Comment 1'}, {user: 'User 2', comment: 'Comment 2'}, {user: 'User 3', comment: 'Comment 3'}]
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />