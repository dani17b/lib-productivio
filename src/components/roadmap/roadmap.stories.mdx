import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { Roadmap } from './Roadmap';

<Meta title="Components/Content/Roadmap" component={Roadmap} />

# Roadmap

Component tha includes information about the users tasks and level ups

<Props of={Roadmap} />

export const Template = (args) => (
  <Roadmap
    taskDate={args.taskDate}
    taskName={args.taskName}
    level={args.level}
    status={args.status}
    style={args.style}
  />
);

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      taskDate: '2023-03-30',
      taskName: 'Tasks name',
      level: 3,
      status: 'EN_CURSO',
      style: {
        dotHeight: 18,
        dotWidth: 18,
        dotColor: '#8394c4',
        lineColor: 'grey',
        lineHeight: 40,
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Roadmap} story="Basic" />
