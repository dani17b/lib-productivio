import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { SearchBar } from './SearchBar';

<Meta title="Components/Content/SearchBar" component={SearchBar} />

# SearchBar

A search bar.
For the results to show, it is neccessary to click outside of the bar, since the function is executed through the onBlur event.

<Props of={SearchBar} />

export const Template = (args) => <SearchBar onSearch={args.onSearch} />;

## Basic

<Canvas>
  <Story
    name="Basic"
    actions={{
      onSearch: 'onSearch',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />
