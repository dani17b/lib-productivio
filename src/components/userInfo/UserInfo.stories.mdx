import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { UserSummary } from './UserSummary';

<Meta title="Components/Content/UserSummary" component={UserSummary} />

# UserSummary

UserSummary placed in the user's profile page. It contains information relative to the user's missions, total points, contacts...

<Props of={UserSummary} />

export const Template = (args) => (
  <UserSummary
    username={args.username}
    userImg={args.userImg}
    userColor={args.userColor}
    totalPoints={args.totalPoints}
    level={args.level}
    contactsNumber={args.contactsNumber}
    currentTasks={args.currentTasks}
  />
);

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      username: 'Users Name',
      userImg: 'Users profile pic',
      userColor: 'Users color based on their level',
      totalPoints: 'Users total points',
      level: 'Users current level',
      contactsNumber: 'Users total contacts',
      currentTasks: 'Users tasks/missions in progress',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={UserSummary} story="Basic" />
