import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { UserDetails } from './UserDetails';

<Meta title="Components/Content/UserDetails" component={UserDetails} />

# UserDetails

UserDetails component shows the username from the user, the profile picture, a short description
and the average score from all projects.

<Props of={UserDetails} />

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      user: {
        username: 'Myusername',
        description: 'This is a brief description from the user',
        score: 4,
        profileColor: 'yellow',
        img: '/src/img/myPic.jpg',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />

export const Template = (args) => <UserDetails user={args.user} />;

