import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { AddIcon } from './AddIcon';


<Meta
    title="Components/Content/AddIcon"
    component={AddIcon}
/>

# AddIcon
Badge component.

<Props of={AddIcon} />

export const Template = (args) => <AddIcon action={args.action}/>;
export const Basic = Template.bind({});
Basic.args = {
  action: () => console.log('Button clicked!'),
};

## Basic

<Canvas>
    <Story
    name="Basic"
    args={{
        action: Basic.args.action
    }}
    >
    {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Basic" />