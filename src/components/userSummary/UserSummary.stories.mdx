import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { UserSummary } from './UserSummary';

<Meta title="Components/Content/UserSummary" component={UserSummary} />

# UserSummary

UserSummary placed in the user profile. It contains - The user's profile picture - User's username - User's current missions/tasks - User's level - User's contacts - User's total score/points

<Props of={UserSummary} />

export const Template = (args) => (
  <LevelProps
    userName={args.userName}
    totalPoints={args.totalPoints}
    level={args.level}
  />
);

export const Template2 = (args) => (
  <ContactsNumberProps contactsNumber={args.contactsNumber} />
);

export const Template3 = (args) => (
  <CurrentTasksProps currentTasks={args.currentTasks} />
);

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      userName: 'UserName',
      totalPoints: 'Users total points',
      level: 'Users current level',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="ContactsNumbers"
    args={{
      contactsNumber: 'Users total contacts',
    }}
  >
    {Template2.bind({})}
  </Story>
  <Story
    name="Tasks"
    args={{
      currentTasks: 'Users tasks/missions in progress',
    }}
  >
    {Template3.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />
<ArgsTable story="ContactsNumbers" />
<ArgsTable story="Tasks" />
