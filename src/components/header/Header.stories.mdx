import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { Header } from './Header';

<Meta 
    title="Components/Menus/Header" 
    component={Header}
/>

# Header
This component is the header that is common to all web screens. At the moment it does not have functionalities, it is only visual. The notifications section has a maximum of 99.

<Props of={Header} />


export const Template = (args) => <Header count={args.count}/>;

## Basic
<Canvas>
  <Story 
    name="Basic"
    args={{
        count : 0
    }}
   >
        {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />











