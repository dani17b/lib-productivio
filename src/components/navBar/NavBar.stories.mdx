import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { NavBar } from './NavBar';
import { FaHome, FaUserFriends, FaUserAlt } from 'react-icons/fa';
import { IoMdAdd, IoMdPodium } from 'react-icons/io';

<Meta 
    title="Components/Content/NavBar" 
    component={NavBar}
/>

# NavBar
This component is the NavBar that is common to all web screens. At the moment it does not have functionalities, it is only visual.

<Props of={NavBar} />

## Basic

<Canvas>
  <Story name="Basic">
    {(props) => (
      <NavBar
        parentBackgroundColor={props.parentBackgroundColor}
        childBackgroundColor={props.childBackgroundColor}
      />
    )}
  </Story>
</Canvas>

<ArgsTable story="Basic" />
