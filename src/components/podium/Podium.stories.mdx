import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { Podium } from './Podium';

<Meta title="Components/Content/Podium" component={Podium} />

# Podium

This component will show a podium with the top 2, 3, and 4 users.

<Props of={Podium} />

export const Template = (args) => (
  <Podium
    userImg={args.userImg}
    username={args.username}
    userColor={args.userColor}
    points={args.points}
  />
);

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      userImg:
        'https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTX48DTma6JzBvRjAy2fQRZMXZmWq5iNlVryn0S7oc&s',
      userColor: 'Users color',
      username: 'ricardo84',
      points: '180',
    }}
  >
    {Template.bind({})}
  </Story>
  <ArgsTable story="Basic" />
</Canvas>
