import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { TaskProgressBar } from './TaskProgressBar';

<Meta title="Components/Content/TaskProgressBar" component={TaskProgressBar} />

# TaskProgressBar

TaskProgressBar component.

<Props of={TaskProgressBar} />

export const Template = (args) => (
  <TaskProgressBar percentage={args.percentage} text={args.text} parentBackgroundColor={args.parentBackgroundColor} childBackgroundColor={args.childBackgroundColor} />
);

## Basic
<Canvas>
  <Story 
    name="0% - NO TEXT"
     argTypes={{
      percentage: 
        { control: { type: 'number', min:0, max:100, step: 1 } } 
      
    }}
   
    args={{
        percentage : 0,
        childBackgroundColor :'red',
        parentBackgroundColor : 'blue'
        
    }}
     
  
   >
        {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="0% - NO TEXT" />

