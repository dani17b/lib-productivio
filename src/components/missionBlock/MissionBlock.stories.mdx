import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { MissionBlock } from './MissionBlock';

<Meta title="Components/Content/MissionBlock" component={MissionBlock} />

# MissionBlock

MissionBlock component.

<Props of={MissionBlock} />

export const Template = (args) => (
  <MissionBlock taskName = {args.taskName} group = {args.group} deadline = {args.deadline} status={ args.status} percentageCompleted={args.percentageCompleted} backgroundColor={args.backgroundColor} progressBarColor={args.progressBarColor} />
);

## Basic
<Canvas>
  <Story 
    name="Basic"   
    args={{
     // task :{
        taskName: "Example Name",
      group: "FP DUAL ALTIA",
      deadline: new Date("2023-06-27T00:00:00.000Z"),
      status: "En Curso",
      percentageCompleted: 80
   //   }
      
    }}
     
  
   >
        {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />
