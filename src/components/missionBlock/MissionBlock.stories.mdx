import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { MissionBlock } from './MissionBlock';

<Meta title="Components/Content/MissionBlock" component={MissionBlock} />

# MissionBlock

MissionBlock component.

<Props of={MissionBlock} />

export const Template = (args) => (
  <MissionBlock task={args.task} backgroundColor={args.backgroundColor} progressBarColor={args.progressBarColor} />
);

## Basic
<Canvas>
  <Story 
    name="Basic"   
    args={{
      task :{
        name: "Example Name",
      team: "FP DUAL ALTIA",
      endDate: "2023-06-29",
      progress: 80
   }
      
    }}
     
  
   >
        {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />
