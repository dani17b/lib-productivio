import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { SelectMenu } from './SelectMenu';

<Meta title="Components/Menus/SelectMenu" component={SelectMenu}/>

# SelectMenu

Dropdown select menu component.

<Props of={SelectMenu} />


## Basic
<Canvas>
  <Story 
    name="Basic"
    args={{
        options : ['Option 1', 'Option 2', 'Option 3'],
        onSelect : (option) => console.log('Selected option:', option),
        label : 'Select an option',
        selectString : 'Opciones',
    }}
   >
        {({options, onSelect, label, selectString, fontSize, color}) => (
            <SelectMenu 
                options={options} 
                onSelect={onSelect} 
                label={label}
                selectString={selectString}
                fontSize={fontSize}
                color={color}
            />
        )}
  </Story>
</Canvas>

<ArgsTable story="Basic" />
