import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { TimeRemaining } from './TimeRemaining';

<Meta title="Components/Content/TimeRemaining" component={TimeRemaining} />

# TimeRemaining

TimeRemaining component.

<Props of={TimeRemaining} />

export const Template = (args) => (
  <TimeRemaining dueTime={args.dueTime} simpleDisplay={args.simpleDisplay}/>
);

## Basic

<Canvas>
  <Story
    name="Default"
    argTypes={{
      dueTime: { control: {  } },
    }}
    args={{
      dueTime: new Date('2023-06-27'),
      simpleDisplay: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
