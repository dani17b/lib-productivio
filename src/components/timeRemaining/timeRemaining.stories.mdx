import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { TimeRemaining } from './TimeRemaining';

<Meta title="Components/Content/TimeRemaining" component={TimeRemaining} />

# TimeRemaining

TimeRemaining component.

export const Template = (args) => (
  <TimeRemaining percentage={args.percentage} text={args.text} />
);

## Basic

<Canvas>
  <Story
    name="Default"
    argTypes={{
      dueTime: { control: { type: 'date' } }
    }}
    args={{
      dueTime: null,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
