import { Meta, Story, Canvas, Props, ArgsTable } from '@storybook/addon-docs';
import { LikeAndComments } from './LikeAndComments';

<Meta title="Components/Content/LikeAndComments" component={LikeAndComments} />

# LikeAndComments

LikeAndComments component.

export const Template = (args) => (
  <LikeAndComments label={args.label} showAny={args.showAny} />
);

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      label: 'Label likeAndComments',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />
